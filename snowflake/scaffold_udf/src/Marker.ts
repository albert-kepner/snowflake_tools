// This file is used as a placeholder in UDF or UDFT folders some of which
// otherwise would have no need to contain typscript files.
// This keeps the build process happy, because we can run the typescript compiler 
// without errors.
export interface Marker {
    description: string;
}