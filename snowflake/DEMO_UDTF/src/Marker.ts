// This file is used as a placeholder in UDF folders which
// otherwise would need to contain no typscript files.
// This keeps the build process happy, because we can run the typescript compiler 
// without errors.
export interface Marker {
    description: string;
}